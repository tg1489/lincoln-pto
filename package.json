{
  "name": "lincoln-pto",
  "version": "1.0.0",
  "description": "",
  "main": "client/index.html",
  "scripts": {
    "heroku-prebuild": "npm install -g pnpm",
    "build": "vite build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cd client && pnpm run dev",
    "watch": "cd server && pnpm run watch",
    "install": "cd client && pnpm i && cd ../server && pnpm i",
    "seed": "cd server/seeds && node user.js",
    "start": "cd server/ && pnpm start",
    "develop": "concurrently \"cd server && pnpm run watch\" \"cd client && pnpm dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.1"
  },
  "devDependencies": {
    "ngrok": "5.0.0-beta.2"
  }
}
